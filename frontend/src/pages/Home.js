import React from 'react';

const Home = () => {

    return (
        <div>
            Home
            {/* {name ? `Hi ${name}` : 'You are not logged in'} */}
        </div>
    );
};

export default Home;